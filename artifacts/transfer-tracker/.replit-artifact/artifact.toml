kind = "web"
previewPath = "/"
title = "Transfer Tracker"
version = "1.0.0"
id = "artifacts/transfer-tracker"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18140

[services.development]
run = "pnpm --filter @workspace/transfer-tracker run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/transfer-tracker", "run", "build" ]
publicDir = "artifacts/transfer-tracker/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18140"
BASE_PATH = "/"
